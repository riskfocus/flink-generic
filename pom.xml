<?xml version="1.0" encoding="UTF-8"?>
<!--
  ~ Copyright 2021-2023 Ness Digital Engineering
  ~
  ~ Licensed under the Apache License, Version 2.0 (the "License");
  ~ you may not use this file except in compliance with the License.
  ~ You may obtain a copy of the License at
  ~
  ~     http://www.apache.org/licenses/LICENSE-2.0
  ~
  ~ Unless required by applicable law or agreed to in writing, software
  ~ distributed under the License is distributed on an "AS IS" BASIS,
  ~ WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  ~ See the License for the specific language governing permissions and
  ~ limitations under the License.
  -->

<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>com.ness.flink.generic</groupId>
    <artifactId>flink-generic</artifactId>
    <version>2.1.0-SNAPSHOT</version>

    <modules>
        <module>flink-domain</module>
        <module>flink-baseline</module>
        <module>flink-example-domain</module>
        <module>flink-test-example</module>
    </modules>

    <packaging>pom</packaging>
    <description>Shared Flink Libraries</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <java.version>17</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${maven.compiler.source}</maven.compiler.target>
        <sonar.organization>ness.com</sonar.organization>
        <lombok.version>1.18.46</lombok.version>
        <guava.version>33.6.0-jre</guava.version>
        <spring.boot.version>3.5.16</spring.boot.version>
        <test.containers.version>2.0.1</test.containers.version>
        <netty.version>4.2.16.Final</netty.version>
    </properties>

    <repositories>
        <repository>
            <id>apache-maven-snapshots</id>
            <name>apache-maven-snapshots</name>
            <url>https://repository.apache.org/content/repositories/snapshots/</url>
        </repository>

        <repository>
            <id>confluentMaven</id>
            <name>confluentMaven</name>
            <url>https://packages.confluent.io/maven/</url>
        </repository>
    </repositories>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.google.guava</groupId>
                <artifactId>guava</artifactId>
                <version>${guava.version}</version>
            </dependency>
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
            </dependency>
            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>6.1.2</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.mockito</groupId>
                <artifactId>mockito-junit-jupiter</artifactId>
                <version>5.23.0</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${test.containers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Lettuce 7 needs Netty 4.2 (uses io.netty.channel.MultiThreadIoEventLoopGroup, absent in
                 4.1). Pin the bom so all unshaded netty transitives resolve to 4.2. -->
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-bom</artifactId>
                <version>${netty.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.junit-pioneer</groupId>
            <artifactId>junit-pioneer</artifactId>
            <version>2.3.0</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <defaultGoal>clean install</defaultGoal>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.14.0</version>
                <configuration>
                    <release>${java.version}</release>
                    <!-- Declare Lombok as an explicit annotation processor: JDK 23+ no longer runs
                         classpath annotation processors implicitly, so without this Lombok does not
                         generate its members and the build fails on JDK 23/25. Harmless on JDK 17. -->
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>3.4.0</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-deploy-plugin</artifactId>
                <version>3.1.4</version>
            </plugin>

            <!-- Exposes each dependency's resolved jar path as a ${groupId:artifactId:type} property,
                 so surefire's argLine can point -javaagent at the mockito-core jar. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>3.8.1</version>
                <executions>
                    <execution>
                        <id>resolve-mockito-agent</id>
                        <phase>initialize</phase>
                        <goals>
                            <goal>properties</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-pmd-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.rat</groupId>
                <artifactId>apache-rat-plugin</artifactId>
                <version>0.18</version>
                <inherited>false</inherited>
                <executions>
                    <execution>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <excludeSubProjects>false</excludeSubProjects>
                    <numUnapprovedLicenses>0</numUnapprovedLicenses>
                    <!-- exclude all licenses to use only fully matched -->
                    <addDefaultLicenseMatchers>false</addDefaultLicenseMatchers>
                    <reportFile>${project.basedir}/license-check.txt</reportFile>
                    <licenses>
                        <license implementation="org.apache.rat.analysis.license.FullTextMatchingLicense">
                            <licenseFamilyCategory>Ness OS</licenseFamilyCategory>
                            <licenseFamilyName>Ness open source</licenseFamilyName>
                            <notes/>
                            <fullText>Copyright 2021-2023 Ness Digital Engineering

                                Licensed under the Apache License, Version 2.0 (the "License");
                                you may not use this file except in compliance with the License.
                                You may obtain a copy of the License at

                                http://www.apache.org/licenses/LICENSE-2.0

                                Unless required by applicable law or agreed to in writing, software
                                distributed under the License is distributed on an "AS IS" BASIS,
                                WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
                                See the License for the specific language governing permissions and
                                limitations under the License.
                            </fullText>
                        </license>
                    </licenses>
                    <licenseFamilies>
                        <licenseFamily
                                implementation="org.apache.rat.license.SimpleLicenseFamily">
                            <familyName>Ness open source</familyName>
                        </licenseFamily>
                    </licenseFamilies>
                    <excludes>
                        <!-- additional files like .gitignore etc.-->
                        <exclude>**/.*/**</exclude>
                        <exclude>**/*.prefs</exclude>
                        <exclude>**/*.json</exclude>
                        <exclude>**/*.log</exclude>
                        <exclude>**/*.iml</exclude>
                        <exclude>**/LICENCE</exclude>
                        <exclude>**/VERSION</exclude>
                        <exclude>**/*.md</exclude>
                        <exclude>**/dependency-reduced-pom.xml</exclude>
                        <exclude>mvnw</exclude>
                        <exclude>mvnw.cmd</exclude>
                        <!-- compiled classes and tests-->
                        <exclude>**/target/**</exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>

        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-pmd-plugin</artifactId>
                    <version>3.28.0</version>
                    <configuration>
                        <printFailingErrors>true</printFailingErrors>
                        <linkXRef>false</linkXRef>
                        <rulesets>
                            <ruleset>.pmd/pmd-check.xml</ruleset>
                        </rulesets>
                    </configuration>
                    <executions>
                        <execution>
                            <goals>
                                <goal>check</goal>
                                <goal>cpd-check</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.5.2</version>
                    <configuration>
                        <forkCount>3</forkCount>
                        <reuseForks>true</reuseForks>
                        <!-- @{argLine} chains the JaCoCo agent (jacoco:prepare-agent); without it no
                             coverage is collected. The mockito-core -javaagent + attach flags let
                             Mockito's inline mock maker instrument on JDK 21+ (self-attach is blocked);
                             harmless on JDK 17. -->
                        <argLine>
                            @{argLine}
                            -javaagent:${org.mockito:mockito-core:jar}
                            -Xmx1024m
                            -Dnet.bytebuddy.experimental=true
                            -Djdk.attach.allowAttachSelf=true
                            -XX:+EnableDynamicAgentLoading
                            --add-opens java.base/java.util=ALL-UNNAMED
                            --add-opens=java.base/java.lang=ALL-UNNAMED
                            --add-opens=java.base/java.lang.reflect=ALL-UNNAMED
                        </argLine>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>0.8.15</version>
                    <configuration>
                        <append>true</append>
                    </configuration>
                    <executions>
                        <execution>
                            <id>unit-test-agent</id>
                            <goals>
                                <goal>prepare-agent</goal>
                            </goals>
                        </execution>
                        <execution>
                            <id>integration-test-agent</id>
                            <goals>
                                <goal>prepare-agent-integration</goal>
                            </goals>
                        </execution>
                        <execution>
                            <id>jacoco-site</id>
                            <phase>package</phase>
                            <goals>
                                <goal>report</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <profiles>
        <!--
            OWASP dependency-check CVE gate. OFF by default so `mvn verify` and every PR build stay
            fast and never flake on NVD API outages. Run explicitly:

                mvn -Powasp dependency-check:aggregate            (scan only, fastest)
                mvn -Powasp verify                                (full build + scan)

            The scheduled .github/workflows/owasp.yml runs it weekly with an NVD_API_KEY secret
            (passed as -Dnvd.api.key=...). Without a key the scan still runs but is heavily
            rate-limited by the NVD API. False positives are suppressed in .owasp/suppressions.xml.
        -->
        <profile>
            <id>owasp</id>
            <properties>
                <!-- Empty by default; CI overrides with -Dnvd.api.key=${{ secrets.NVD_API_KEY }} -->
                <nvd.api.key></nvd.api.key>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.owasp</groupId>
                        <artifactId>dependency-check-maven</artifactId>
                        <version>12.2.2</version>
                        <configuration>
                            <!-- Fail the build on any dependency with a CVE scoring CVSS >= 7 (High/Critical) -->
                            <failBuildOnCVSS>7</failBuildOnCVSS>
                            <!-- Strict gate: fail the build if the scan itself cannot complete, not just
                                 on the CVSS threshold. This avoids a silent false-green from an incomplete
                                 NVD load. Safe now that 12.2.2 fixed the VARCHAR(1000) URL crash (PR #8467);
                                 a genuine future tool/NVD error will surface as a red run to investigate,
                                 rather than being swallowed. -->
                            <failOnError>true</failOnError>
                            <nvdApiKey>${nvd.api.key}</nvdApiKey>
                            <suppressionFiles>
                                <suppressionFile>.owasp/suppressions.xml</suppressionFile>
                            </suppressionFiles>
                            <formats>
                                <format>HTML</format>
                                <format>JSON</format>
                            </formats>
                            <!-- Assembly analyzer needs .NET; irrelevant to a JVM project and noisy -->
                            <assemblyAnalyzerEnabled>false</assemblyAnalyzerEnabled>
                            <!-- OSS Index requires an authenticated account; unauthenticated it is
                                 rate-limited and errors on every artifact. NVD + RetireJS are enough. -->
                            <ossindexAnalyzerEnabled>false</ossindexAnalyzerEnabled>
                        </configuration>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>aggregate</goal>
                                </goals>
                                <phase>verify</phase>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
